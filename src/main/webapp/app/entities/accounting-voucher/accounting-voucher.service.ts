import axios from 'axios';

import buildPaginationQueryOpts from '@/shared/sort/sorts';

import { IAccountingVoucher } from '@/shared/model/accounting-voucher.model';

const baseApiUrl = 'api/accounting-vouchers';

export default class AccountingVoucherService {
  public find(id: number): Promise<IAccountingVoucher> {
    return new Promise<IAccountingVoucher>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public retrieve(paginationQuery?: any): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(baseApiUrl + `?${buildPaginationQueryOpts(paginationQuery)}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public delete(id: number): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public create(entity: IAccountingVoucher): Promise<IAccountingVoucher> {
    return new Promise<IAccountingVoucher>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public update(entity: IAccountingVoucher): Promise<IAccountingVoucher> {
    return new Promise<IAccountingVoucher>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
